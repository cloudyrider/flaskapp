apiVersion: v1
kind: Pod
metadata:
  name: flaskpod
spec:
  containers:
  - name: flaskapp
    image: 도커허브계정/flaskapp:latest
    ports:
    - containerPort: 5000
  imagePullSecrets:
  - name: docker-pull-secret